syntax = "proto3";

package chat;

service ChatService {
    rpc ChatStream (stream ChatMessage) returns (stream ChatMessage);
}

message ChatMessage {
    string message = 1;
    string from = 2;
    int64 timestamp = 3;
}